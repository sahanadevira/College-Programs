#include <stdio.h>
int main()
{
    int N, i, S;
    printf("Enter the N value:");
    scanf("%d", &N);
    i = 1;
    S = 0;
    while (i <= N)
    {
        S = S + i;
        i = i + 1;
    }
    printf("The sum is %d", S);
    return 0;
}
