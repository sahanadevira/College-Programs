#include <stdio.h>
#include <math.h>
int main()
{
    int N, C=0, S=0, num;
    printf("Enter number:");
    scanf("%d", &N);
    while (N > 0)
    {
        C = C + 1;
        N = N;
        num = N % 10;
        while (N > 0)
        {
            S = S + pow(num, C);
        }
        N = N / 10;
    }
    if (num == S)
        printf("The num %d is an Armstrong number", n);
    else
        printf("The num %d is not an Armstrong number", n);
    return 0;
}
